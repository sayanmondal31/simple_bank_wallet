services:
  db:
    image: postgres:16
    restart: always
    shm_size: 128mb

    volumes:
      - /Users/sayanmondal/Documents/Learnings/go_learn/go_projects/simple_bank/postgres_data:/var/lib/postgresql/data

    environment:
      POSTGRES_PASSWORD: secret
      POSTGRES_USER: root
      POSTGRES_DB: simple_bank
    ports:
      - 5432:5432
